from django.urls import path
from . import views

# urlpatterns = [
#     path('game/<int:game_id>/state', views.getGameState, name='get_game_state'),
#     path('game/<int:game_id>/update', views.updateGameState, name='update_game_state'),
# ]
